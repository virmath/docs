# 快速入门

![](quick-start/1.webp)
![](quick-start/2.webp)

1. 每个程序有且只有一个`控制流/程序结束`节点，可以通过画布右上角功能按钮快速定位到该节点。
2. 除了`控制流/分支`节点和`控制流/循环`类节点，其它节点皆从左到右，从上到下依次执行。
3. 除了连接至`循环体`的节点，其它节点只会执行一次（即使同个输出端连接到多个不同的节点）。